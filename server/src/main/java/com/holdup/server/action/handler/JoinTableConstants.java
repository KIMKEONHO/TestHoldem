package com.holdup.server.action.handler;

import java.math.BigDecimal;

/** 테이블 입장/착석 시 기본 스택. */
final class JoinTableConstants {
    static final BigDecimal DEFAULT_STACK = BigDecimal.valueOf(1000);
    private JoinTableConstants() {}
}
